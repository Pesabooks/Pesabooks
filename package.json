{
  "private": true,
  "name": "pesabooks",
  "workspaces": [
    "src/*"
  ],
  "scripts": {
    "build:ui": "yarn workspace @pesabooks/ui build",
    "build:contracts": "yarn workspace @pesabooks/contracts compile",
    "build": "yarn build:contracts && yarn build:ui"
  },
  "devDependencies": {
    "ts-node": "^10.4.0"
  }
}
